This directory holds the tests code that will test the code present in the code directory.

Run the below command from project's root directory to run all tests present in the project.
```
python -m pytest
```